{%- if settings.sb_account_placement == 'left' -%}
<div class="col-md-3{% if settings.sb_account_mobile %} hidden-sm hidden-xs{% endif %}">
  {%- include 'sidebar-myaccount' -%}
</div>
{%- endif -%}
<div id="content" class="{% if settings.sb_account_placement != '' %}col-md-9{% else %}col-md-12{% endif %} center-column body-other content-with-background">
  {%- form 'reset_customer_password' -%}
  {%- if form.errors -%}
  <div class="warning">
    <button type="button" class="close" data-dismiss="alert">×</button>
    {{form.errors | default_errors}}
  </div>
  {%- endif -%}
  <fieldset id="account" class="form-horizontal">
    <legend>{{'customer.reset_password.title' | t}}</legend>
    <p><em>{{'customer.reset_password.subtext' | t:email:email}}</em></p>
    <div class="form-group required">
      <label class="col-sm-2 control-label" for="input-password">{{'customer.reset_password.password' | t}}</label>
      <div class="col-sm-10">
        <input type="password" required="required" name="customer[password]" placeholder="{{'customer.reset_password.password' | t}}" id="input-password" class="form-control" />
      </div>
    </div>
    <div class="form-group required">
      <label class="col-sm-2 control-label" for="input-password2">{{'customer.reset_password.password_confirm' | t}}</label>
      <div class="col-sm-10">
        <input type="password" required="required" name="customer[password_confirmation]" placeholder="{{'customer.reset_password.password_confirm' | t}}" id="input-password2" class="form-control" />
      </div>
    </div>
  </fieldset>
  <div class="buttons">
    <input type="submit" value="{{'customer.reset_password.submit' | t}}" class="btn btn-primary" />
  </div>
  {%- endform -%}
</div>
{%- if settings.sb_account_placement == 'right' -%}
<div class="col-md-3{% if settings.sb_account_mobile %} hidden-sm hidden-xs{% endif %}">
  {%- include 'sidebar-myaccount' -%}
</div>
{%- endif -%}